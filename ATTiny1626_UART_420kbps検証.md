# ATTiny1626 UART 420000bps対応検証

## ATTiny1626のUART仕様

### 基本仕様
- **CPU速度**: 最大20MHz
- **UART**: USART（Universal Synchronous/Asynchronous Receiver/Transmitter）
- **ボーレートジェネレーター**: 内蔵

## ボーレート計算

### AVR UARTのボーレート計算式

#### 通常モード（Normal Mode）
```
BAUD = f_OSC / (16 × (UBRR + 1))
```

#### 2倍速度モード（Double Speed Mode）
```
BAUD = f_OSC / (8 × (UBRR + 1))
```

### 20MHzクロックでの420000bps計算

#### 通常モード
```
UBRR = (f_OSC / (16 × BAUD)) - 1
UBRR = (20000000 / (16 × 420000)) - 1
UBRR = (20000000 / 6720000) - 1
UBRR = 2.976... - 1
UBRR = 1.976...
```

**UBRRは整数値なので、最も近い値:**
- UBRR = 2 → BAUD = 20000000 / (16 × 3) = 416,666.67 bps
- UBRR = 1 → BAUD = 20000000 / (16 × 2) = 625,000 bps

**誤差:**
- UBRR = 2の場合: 416,666.67 / 420,000 = 0.992... (約0.8%の誤差)

#### 2倍速度モード
```
UBRR = (f_OSC / (8 × BAUD)) - 1
UBRR = (20000000 / (8 × 420000)) - 1
UBRR = (20000000 / 3360000) - 1
UBRR = 5.952... - 1
UBRR = 4.952...
```

**UBRRは整数値なので、最も近い値:**
- UBRR = 5 → BAUD = 20000000 / (8 × 6) = 416,666.67 bps
- UBRR = 4 → BAUD = 20000000 / (8 × 5) = 500,000 bps

**誤差:**
- UBRR = 5の場合: 416,666.67 / 420,000 = 0.992... (約0.8%の誤差)

## 誤差の許容範囲

### UART通信の一般的な誤差許容範囲
- **許容誤差**: 通常±2～3%以内
- **推奨誤差**: ±1%以内

### 計算結果
- **通常モード**: 約0.8%の誤差 → ✅ **許容範囲内**
- **2倍速度モード**: 約0.8%の誤差 → ✅ **許容範囲内**

## CRSFプロトコルでの影響

### CRSF v3の仕様
- **ボーレート**: 420000bps（推奨）
- **許容誤差**: プロトコル仕様によるが、通常±2%程度は許容される

### 実測ボーレート
- **設定値**: 416,666.67 bps（UBRR = 2、通常モード）
- **誤差**: 約0.8%
- **判定**: ✅ **CRSF通信に問題なし**

## 結論

### ATTiny1626は420000bpsに対応可能か？

**答え: ✅ はい、対応可能です**

**理由:**
1. 20MHzクロックでUBRR = 2（通常モード）またはUBRR = 5（2倍速度モード）を使用
2. 実測ボーレート: 416,666.67 bps
3. 誤差: 約0.8%（許容範囲内）
4. CRSFプロトコルでの通信に問題なし

### 推奨設定

```c
// ATTiny1626での420000bps設定例（通常モード）
// 20MHzクロック使用時
#define F_CPU 20000000UL
#define BAUD 420000
#define UBRR_VAL ((F_CPU / (16UL * BAUD)) - 1)  // UBRR = 2

// USART0の設定
UBRR0 = 2;  // ボーレートレジスタ
UCSR0A = 0;  // 通常モード
UCSR0B = (1 << RXEN0) | (1 << TXEN0);  // 送受信有効
UCSR0C = (1 << UCSZ01) | (1 << UCSZ00);  // 8bitデータ
```

### 他のクロック周波数での検証

#### 16MHzクロック（内部クロック使用時）
```
通常モード: UBRR = (16000000 / (16 × 420000)) - 1 = 1.38... → UBRR = 1
BAUD = 16000000 / (16 × 2) = 500,000 bps（誤差が大きい）

2倍速度モード: UBRR = (16000000 / (8 × 420000)) - 1 = 3.76... → UBRR = 4
BAUD = 16000000 / (8 × 5) = 400,000 bps（誤差: 約4.8%）
```
**結論**: 16MHzでは420000bpsに近い値が得られない → ❌ **非推奨**

#### 24MHzクロック（外部クロック使用時）
```
通常モード: UBRR = (24000000 / (16 × 420000)) - 1 = 2.57... → UBRR = 3
BAUD = 24000000 / (16 × 4) = 375,000 bps（誤差が大きい）

UBRR = 2の場合: BAUD = 24000000 / (16 × 3) = 500,000 bps（誤差が大きい）

2倍速度モード: UBRR = (24000000 / (8 × 420000)) - 1 = 6.14... → UBRR = 6
BAUD = 24000000 / (8 × 7) = 428,571.43 bps（誤差: 約2.0%）
```
**結論**: 24MHzでは2倍速度モードで約2%の誤差 → ⚠️ **やや大きい誤差**

#### 20MHzクロック（推奨）
```
通常モード: UBRR = 2 → BAUD = 416,666.67 bps（誤差: 約0.8%）✅
2倍速度モード: UBRR = 5 → BAUD = 416,666.67 bps（誤差: 約0.8%）✅
```
**結論**: 20MHzが最適 → ✅ **推奨**

### 実装時の推奨事項

1. **外部クロックの使用**
   - 20MHzの外部水晶発振子またはセラミック発振子を使用
   - 内部クロックは精度が低いため非推奨

2. **ボーレート設定**
   - 通常モード: UBRR = 2（推奨）
   - 2倍速度モード: UBRR = 5（同等の精度）

3. **誤差の影響**
   - 約0.8%の誤差はCRSF通信に問題なし
   - フレーム同期は0xC8で開始するため、誤差による影響は小さい

4. **テスト方法**
   - CRSF信号を実際に受信して動作確認
   - オシロスコープでボーレートを測定
   - エラーレートの確認

### 注意事項
- **クロック精度**: 外部クロックまたは高精度な内部クロックの使用を推奨
- **誤差の累積**: 長時間通信では誤差の累積に注意（ただし0.8%の誤差では問題なし）
- **テスト**: 実際のCRSF信号での動作確認を推奨
- **クロック周波数**: 20MHzの外部クロック使用を強く推奨（16MHzでは非対応）
