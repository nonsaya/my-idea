# EdgeTXスプラッシュスクリーン トラブルシューティング

## スプラッシュスクリーンが表示されない場合

### 1. EdgeTXの設定メニューで選択する

**最も重要なポイント**: EdgeTXでは、スプラッシュスクリーンファイルを`SPLASH`フォルダに配置するだけでは表示されません。
**設定メニューで明示的に選択する必要があります。**

手順:
1. EdgeTXのメインメニューを開く
2. `RADIO SETUP`（または`SETUP`）を選択
3. `Splash Screen`（または`スプラッシュスクリーン`）を選択
4. ファイル一覧から作成したスプラッシュスクリーンファイルを選択
5. 保存してトランスミッターを再起動

### 2. ファイル名の確認

EdgeTXのバージョンによっては、ファイル名の先頭にスペースが必要な場合があります。

試すべきファイル名:
- ` splash.bmp`（先頭にスペース）
- `splash.bmp`（スペースなし）
- `splash.png`（PNG形式）

### 3. ファイル形式の確認

- **BMP形式を推奨**: EdgeTXの多くのバージョンでBMP形式の方が互換性が高いです
- PNG形式が認識されない場合は、BMP形式で再作成してください

```bash
python3 create_splash.py --text "My Radio" --format bmp --output splash.bmp
```

### 4. 画像サイズの確認

トランスミッターの種類に応じたサイズを使用してください:
- **カラースクリーン**: 212x64ピクセル
- **モノクロスクリーン**: 128x64ピクセル

```bash
# カラースクリーン用
python3 create_splash.py --text "My Radio" --output splash.bmp

# モノクロスクリーン用
python3 create_splash.py --mono --text "My Radio" --output splash.bmp
```

### 5. ファイルの配置場所の確認

- SDカードのルートに`SPLASH`フォルダを作成
- ファイルを`SPLASH`フォルダに配置
- ファイル名は大文字小文字を区別する場合があります

### 6. EdgeTXのバージョン確認

EdgeTXのバージョンによって、スプラッシュスクリーンの扱いが異なる場合があります。
最新バージョンに更新することを検討してください。

### 7. SDカードのフォーマット確認

SDカードが正しくフォーマットされているか確認してください:
- FAT32形式である必要があります
- SDカードが破損していないか確認

## よくある問題と解決方法

### 問題: デフォルトのEdgeTXロゴが表示される

**原因**: 設定メニューでスプラッシュスクリーンを選択していない

**解決方法**:
1. EdgeTXの設定メニュー（RADIO SETUP → Splash Screen）を開く
2. ファイル一覧から作成したファイルを選択
3. 保存して再起動

### 問題: ファイルが一覧に表示されない

**原因**: 
- ファイル名が正しくない
- ファイル形式がサポートされていない
- ファイルサイズが大きすぎる

**解決方法**:
1. BMP形式で再作成
2. ファイル名を` splash.bmp`（先頭スペース付き）に変更
3. 画像サイズを確認（212x64または128x64）

### 問題: 画像が表示されるが、文字が読めない

**原因**: 
- 背景色とテキスト色のコントラストが低い
- フォントサイズが適切でない

**解決方法**:
```bash
# 白背景・黒文字で作成
python3 create_splash.py --text "My Radio" --bg-color white --text-color black --output splash.bmp

# または黒背景・白文字で作成
python3 create_splash.py --text "My Radio" --bg-color black --text-color white --output splash.bmp
```

## 確認チェックリスト

- [ ] `SPLASH`フォルダがSDカードのルートに存在する
- [ ] ファイルが`SPLASH`フォルダに正しく配置されている
- [ ] ファイル名が正しい（先頭スペースの有無を確認）
- [ ] ファイル形式がBMPまたはPNGである
- [ ] 画像サイズが正しい（212x64または128x64）
- [ ] EdgeTXの設定メニューでスプラッシュスクリーンを選択した
- [ ] トランスミッターを再起動した
