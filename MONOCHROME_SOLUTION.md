# モノクロスクリーン（GX12）でカスタムスプラッシュスクリーンを表示する方法

## 現状の問題

EdgeTX 2.11.0のソースコードでは、モノクロスクリーン用のコード（`gui/128x64/splash.cpp`）にSDカードから画像を読み込む処理が実装されていません。

## 解決方法

### 方法1: EdgeTXのファームウェアをカスタマイズする（上級者向け）

モノクロスクリーン用のコードを修正して、SDカードから画像を読み込む機能を追加することができます。

#### 実装手順

1. **EdgeTXのソースコードを取得**
   ```bash
   git clone https://github.com/EdgeTX/edgetx.git
   cd edgetx
   ```

2. **`radio/src/gui/128x64/splash.cpp`を修正**

   現在のコード：
   ```cpp
   void drawSplash()
   {
     lcdClear();
     lcdDraw1bitBitmap(0, 0, splash_lbm, 0, 0);
     lcdRefresh();
   }
   ```

   修正後のコード（例）：
   ```cpp
   void drawSplash()
   {
     lcdClear();
     
     // SDカードから画像を読み込む処理を追加
     if (sdMounted()) {
       // SDカードからsplash.bmpを読み込む処理
       // （実装が必要）
     }
     
     // SDカードから読み込めない場合はデフォルトを表示
     if (/* 画像が読み込めなかった場合 */) {
       lcdDraw1bitBitmap(0, 0, splash_lbm, 0, 0);
     }
     
     lcdRefresh();
   }
   ```

3. **ファームウェアをビルド**
   ```bash
   ./companion/fw/build.sh
   ```

4. **ファームウェアをフラッシュ**

   カスタマイズしたファームウェアをトランスミッターに書き込みます。

**注意**: この方法は技術的な知識が必要で、ファームウェアのビルド環境のセットアップが必要です。

### 方法2: EdgeTXの最新バージョンを確認

EdgeTXの最新バージョンで、モノクロスクリーン用のカスタムスプラッシュスクリーン機能が追加されている可能性があります。

1. EdgeTXの最新バージョンを確認
2. 最新バージョンに更新
3. 機能が追加されているか確認

### 方法3: EdgeTXコミュニティに機能リクエスト

この機能が必要な場合は、EdgeTXのGitHubリポジトリで機能リクエストを提出してください：

1. EdgeTX GitHub: https://github.com/EdgeTX/edgetx
2. Issues セクションで機能リクエストを作成
3. 「モノクロスクリーン対応トランスミッターでのカスタムスプラッシュスクリーン機能」をリクエスト

### 方法4: 212x64スクリーン用のコードを参考にする

`gui/212x64/splash.cpp`を確認して、212x64スクリーン用の実装を参考にすることができます。

### 方法5: ビルトインのスプラッシュスクリーンを置き換える（高度）

EdgeTXのビルド時に、ビルトインのスプラッシュスクリーンデータ（`bitmaps/128x64/splash.lbm`）を自分の画像に置き換えることができます。

1. 自分の画像をLBM形式に変換
2. `bitmaps/128x64/splash.lbm`を置き換え
3. ファームウェアをビルド

## 推奨される方法

### 短期間の解決策

1. **EdgeTXの最新バージョンを確認** - 機能が追加されている可能性があります
2. **EdgeTXフォーラムで確認** - 他のユーザーが同様の問題を解決しているか確認

### 長期的な解決策

1. **機能リクエストを提出** - EdgeTXコミュニティに機能をリクエスト
2. **ファームウェアをカスタマイズ** - 技術的に可能であれば、自分で実装

## 実装の詳細（方法1の補足）

モノクロスクリーン用にSDカードから画像を読み込む機能を追加する場合、以下のような実装が必要です：

1. **SDカードのマウント確認**
2. **画像ファイルの読み込み**（BMP形式の1ビットモノクロ画像）
3. **画像の表示処理**
4. **エラーハンドリング**（ファイルが見つからない場合のフォールバック）

実装には、EdgeTXのSDカード読み込みAPIとLCD描画APIの知識が必要です。

## 注意事項

- ファームウェアのカスタマイズは、トランスミッターをブリックするリスクがあります
- 必ずバックアップを取ってから作業してください
- EdgeTXのライセンス（GPLv2）に従う必要があります
